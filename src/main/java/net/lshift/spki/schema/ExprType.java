package net.lshift.spki.schema;

import net.lshift.spki.convert.Convert;
import net.lshift.spki.convert.SexpBacked;

@Convert.ByPosition(name="expr", fields={})
public class ExprType 
extends SexpBacked 
implements ConverterDeclaration {

}
